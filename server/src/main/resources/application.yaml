spring:
  application:
    name: server
  datasource:
    url: jdbc:h2:mem:learnow
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true

  security:
    user:
      name: admin
      password: 123456
